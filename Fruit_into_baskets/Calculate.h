#pragma once
#include <vector>
#include <set>
class Calculate
{
	bool hasLotNums(std::vector<int>& arr);
	int getLine(int j, std::vector<int>& arr);
public:
	int totalFruit(std::vector<int>& fruits);

};

